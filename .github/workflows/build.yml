name: Build ResolveRelay

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm run build
      - name: Validate Netlify backup function
        run: npx esbuild netlify/functions/api.mts --bundle --platform=node --format=esm --outfile=/tmp/resolverelay-netlify-api.mjs
      - name: Validate Cloudflare Pages function
        run: npx esbuild 'functions/api/[[path]].ts' --bundle --platform=browser --format=esm --outfile=/tmp/resolverelay-cloudflare-api.mjs
      - name: Wait for Netlify production deploy
        if: github.event_name == 'push'
        run: sleep 45
      - name: Check production API health
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          body="$(curl -fsS --retry 3 --retry-delay 5 https://resolverelai.netlify.app/api/health)"
          echo "$body"
          echo "$body" | grep -q '"ok":true'
      - name: Check production AI route
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          response_file="$(mktemp)"
          status="$(curl -sS --retry 2 --retry-delay 4 -o "$response_file" -w '%{http_code}' \
            -H 'Content-Type: application/json' \
            -d '{"task":"next_step","lang":"en","claim":{"title":"Smoke test claim","description":"Item was not delivered","requested_resolution":"full_refund","status":"prepared"}}' \
            https://resolverelai.netlify.app/api/claim-assist)"
          cat "$response_file"
          test "$status" = "200"
          grep -q '"text"' "$response_file"
